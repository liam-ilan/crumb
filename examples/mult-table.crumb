(print (reduce (map (map (range 10) {row y ->
  return (map (range 10) {item x ->
    return (multiply (add y 1) (add x 1))
  })
}) {row y ->
  return (reduce row {acc item i -> 
    return (join (string acc) " " (string item))
  })
}) {acc row y ->
  return (join acc "\n" row)
}))
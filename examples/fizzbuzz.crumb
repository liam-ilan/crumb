(loop 100 {i -> 
  (if (is (remainder i 15) 0) {
    (print "fizzbuzz")
  } {
    (if (is (remainder i 3) 0) {
      (print "fizz")
    } {
      (if (is (remainder i 5) 0) {
        (print "buzz")
      } {
        (print i)
      })
    })
  })
})